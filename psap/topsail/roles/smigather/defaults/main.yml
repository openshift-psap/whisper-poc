# Auto-generated file, do not edit manually ...
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: Smigather.main

# Parameters
# the namespace where the dcgm exporter container is running
smigather_dcgm_namespace: nvidia-gpu-operator

# the label of the pod running the dcgm exporter
smigather_dcgm_pod_label: nvidia-dcgm-exporter

# the dcgm container suffix
smigather_container_name_suffix: nvidia-dcgm-exporter

# the interval for retrieving data from the dcgm exported container
smigather_monitor_interval: 10

# the output file when retrieving data from the dcgm container
smigather_container_output_file: /tmp/gpu_metrics.csv

# the output file relative to the playbook execution
smigather_local_output_file: ./whisper_bench-output/gpu_metrics.csv

# the folder path relative to the playbook execution
smigather_output_folder_path: ./whisper_bench-output

# the namespace of the pod running the benchmark
smigather_command_pod_namespace: my-whisper-runtime

# the name of the pod running the benchmark
smigather_command_pod_name: vllm-standalone
